---
description: Check APV with compact results and detailed local diagnostics, optionally using the checker agent.
---

Read and follow `${CLAUDE_PLUGIN_ROOT}/skills/apv-check/SKILL.md`.
Use the checker agent when available; otherwise run the same script directly.
Pass only requested check flags from $ARGUMENTS. Never capture, repair or commit
as part of this command.
